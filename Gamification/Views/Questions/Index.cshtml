@model IEnumerable<Gamification.Models.Question>

@{
    ViewBag.Title = "Pitanja";
}

<h2>Pitanja</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quiz.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Where(q=>q.QuizId == ViewBag.QuizId)){
    if (!item.Answers.Any())
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quiz.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id }) |*@
            @Html.ActionLink("Dodaj odgovore", "Create", "Answers", new { id = item.Id }, null)
        </td>
    </tr>
    }
}

</table>
